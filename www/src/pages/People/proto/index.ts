export interface PeopleList {
  id: number;
  name: string;
  url: string;
  sid: string;
  create_time: string;
}

export interface ListQuest {
  keywords: string;
  page: string;
  pagesize: string;
}