import * as React from 'react';
import Layout from '../../components/Layout'


const Project = (props) => {
  return (
    <Layout {...props}>
      Project
    </Layout>
  )
}

export default Project